---
class: Jarvis::Jabber
init: 
  alias: ${hostname}_xmpp
  ip: ${hostname}.${domain}
  port: 5222
  hostname: ${domain}
  username: ${hostname}
  password: ${ENV{'XMPP_PASSWORD'}}
  channel_list: 
    - asgard\@conference.websages.com/${hostname}
  persona: system

